[package]
name = "api"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = "1.0.130"
types = { path = "../types" }

[dependencies.rocket]
rocket = "0.5.0-rc.2"
features = ["json"]
